"use client";

/**
 * Sidebar derecha del detalle del interesado.
 * Misma estructura visual que PropertyRightSidebar pero sin contenido (reservado para futuro).
 */
export function LeadRightSidebar() {
  return (
    <div className="w-full lg:w-80 border-l-0 lg:border-l bg-white dark:bg-[var(--vistral-gray-900)] rounded-lg border border-[var(--vistral-gray-200)] dark:border-[var(--vistral-gray-800)] shadow-sm">
      <div className="p-6">
        {/* Reservado para información del interesado */}
      </div>
    </div>
  );
}
